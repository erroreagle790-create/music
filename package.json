{
  "name": "backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",          // 🟢 Render uses this
    "dev": "nodemon server.js",         // 🔄 For local dev (optional)
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Error Eagle",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",                // ✅ Use this instead of "env"
    "express": "^4.21.1",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.8.2",
    "multer": "^1.4.5-lts.1",
    "zod": "^3.23.8"
  },
  "engines": {
    "node": "18.x"                      // 🔧 Optional: set node version
  }
}

